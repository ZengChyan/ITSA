#include <iostream>
#include <stdio.h>
#include <cmath>
#include <algorithm>

using namespace std;

int main(){

    int num, sum = 0;  
    for(int i = 0; i < 5; i++)  
    {  
        cin >> num;  
        sum +=((num+1) % 13);  
    }  
    cout << sum << endl;  

    return 0;
}
